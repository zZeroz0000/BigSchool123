
@{
    ViewBag.Title = "Mine";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="row">
    <ul class="course">
        @foreach (var course in Model)
        {
            <li>
                <div class="date">
                    <div class="month">
                        @course.DateTime.ToString("MM")
                    </div>
                    <div class="day">
                        @course.DateTime.ToString("dd")
                    </div>
                </div>
                <div class="details">
                    <div class="lecture">
                        @course.LectureName
                    </div>
                    <span class="category">
                        @course.Category.Name
                    </span>
                </div>
                <div class="action">
                    <a href="@Url.Action("Edit","Courses",new { id = course.Id })">Edit</a>
                    <a href="@Url.Action("Detele","Courses",new { id = course.Id })">Delete</a>
                </div>
            </li>
        }
    </ul>
</div>

